import React, {useEffect, useRef} from 'react';
import {Redirect, withRouter} from "react-router";



const AdmEditCapCakeCategory = (props) => {

  return (
    <div className="admin__frame">
  Add new category
    </div>
  )
}

export default withRouter(AdmEditCapCakeCategory);
